# Next Release — Draft Notes

Running draft of unreleased, customer-facing changes. Appended to automatically
by `/phase-done` when a phase wraps, and by hand for ad hoc work in between.
`/release` uses this file as the starting draft and clears it back to empty
once that release's draft has been created.

## Mobile Fixes

- Fixed restaurant list buttons running off the edge of the screen on phones
- Event creation form now fits properly on phone screens instead of getting cut off
- Walk-in attendance search now scrolls into view automatically and no longer
  suggests people who are already checked in
