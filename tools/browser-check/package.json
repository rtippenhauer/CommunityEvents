{
  "name": "dinnerbears-browser-check",
  "private": true,
  "version": "1.0.0",
  "description": "Headless-browser page checks against stage/prod, authenticated as the automation account",
  "scripts": {
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
