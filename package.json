{
  "name": "communityevents",
  "private": true,
  "version": "1.5.1",
  "description": "Repo-root workspace. Holds only the cross-cutting Playwright e2e suite; api/ and frontend/ each have their own package.json.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
